package com.example.firedatabase_assis

/*

class LoadfromButton : AppCompatActivity() {
    fun loadSavedContainers() {
        while (imageContainers.isNotEmpty()) {
            val wrapper = imageContainers.poll() // Get the top container from the priority queue
            if (wrapper.savedState) {
                // If savedState is true, load the container
                loadImageFromUrl(baseURL, wrapper.imageView!!)
            }
        }
    }
}

 */